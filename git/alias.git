# @see Local `./.git/config`, Global `$HOME/.gitconfig`, System `/etc/gitconfig`
# git config --list --show-origin --show-scope
[alias]
	quick-stats = ! /usr/local/bin/git-quick-stats
	st = status
	a = add
	co = checkout
	df = diff
	d = diff

